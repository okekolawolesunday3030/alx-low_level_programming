#include "main.h"

/**
 * -isupper.c - function that checks for uppercase character
 *  @c: parameter
 *  Return: 1 if c is uppercase or otherwise
 */

int _isupper(int c)
{
	if (c = 'a' && c <= 'z')
	{
		_putchar(1);
	}
	else
	{
		_putchar(0);
	}
}
