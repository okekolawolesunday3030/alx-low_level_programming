#include "main.h"
#include <string.h>

/**
 * _strlen_recursion - function returns the length of string
 * @s: parameter used
 */
int _strlen_recursion(char *s)
{
	int length ;

	if (*s)
	
	length = strlen(s);
	return (length);
}
