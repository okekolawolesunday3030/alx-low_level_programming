#include <stdio.h>

/** 
 * main - starting point of the program
 *
 * Description: prints a sample string
 * Return: 0 if program executes properly, else a non-zero number
 */
int main(void)
{
	 char doo = "\"Programming is like building a multilingual puzzle";
	puts(doo);
	return (0)
}
